[] Open up with Claude Code
[] Ask Claude Code to read the `*.md` file under `.claude\docs` and then create a `CLAUDE.md` file in the root
[] Then tell CLAUDE that every conversation has to be logged under `.claude\chats` and checked in
[] Build this application with Claude Code. Your task is to use Claude Code to plan, discussion, iterate on anything you don't understand, get clarity on what Claude Code is going to do, per task, so that you can learn as you.